package com.root.repository.mart;

import com.root.entity.mart.PoliceStationMart;
import org.springframework.data.jpa.repository.JpaRepository;

public interface PoliceStationRepository extends JpaRepository<PoliceStationMart, Long> {
}
