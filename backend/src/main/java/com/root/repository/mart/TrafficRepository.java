package com.root.repository.mart;

import com.root.entity.mart.TrafficMart;
import org.springframework.data.jpa.repository.JpaRepository;

public interface TrafficRepository extends JpaRepository<TrafficMart, Long> {
}
